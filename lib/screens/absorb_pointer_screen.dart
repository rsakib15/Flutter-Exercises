import 'package:flutter/material.dart';

class AbsorbPointerPractice extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("AbsorbPointer Parctice"),
      ),
      body: SafeArea(
        child: Container(),
      ),

    );
  }
}
